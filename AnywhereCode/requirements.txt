# All pure Python — no Rust/C toolchain, so `pip install` works on Termux.
requests>=2.28.0
click>=8.0.0
pyyaml>=6.0
rich>=13.0.0
qrcode>=7.4          # terminal QR codes for installing built APKs

# Optional: only needed for `anyplace serve` (MCP server)
# Install with: pip install mcp>=1.0.0
# mcp>=1.0.0
